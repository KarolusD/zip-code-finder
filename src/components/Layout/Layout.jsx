import React from 'react';
import SearchBar from '../SearchBar';
import Table from '../Table';

const Layout = () => {
  return (
    <>
      <SearchBar />
      <Table />
    </>
  );
};

export default Layout;
